---
/**
 * Componente: Hero
 *
 * Sección principal de bienvenida (Above the Fold).
 *
 * INTEGRACIÓN CON WORDPRESS:
 * - Este contenido suele ser estático o gestionado desde una página de "Opciones del Tema" (ACF Options Page).
 * - Campos sugeridos:
 *   - Título Principal ("Impulsando el futuro...")
 *   - Subtítulo/Descripción ("Unificamos academia...")
 *   - Imagen de Fondo (Media Upload)
 *   - Texto y Enlace del Botón Principal
 *   - Texto y Enlace del Botón Secundario
 */
import { Image } from "astro:assets";
import heroBg from "../assets/hero-bg.jpg";
import { Icon } from "astro-icon/components";
---

<section
  id="home"
  class="relative h-[100svh] flex items-center justify-center text-center px-4 overflow-hidden"
>
  <!-- Imagen de Fondo Optimizada -->
  <Image
    src={heroBg}
    alt=""
    role="presentation"
    aria-hidden="true"
    class="absolute inset-0 w-full h-full object-cover -z-20"
    quality="low"
    loading="eager"
    fetchpriority="high"
    format="webp"
    widths={[640, 768, 1024, 1280, 1536, 1920]}
    sizes="100vw"
  />

  <!-- Capa de Gradiente (Replicando el diseño original) -->
  <div
    class="absolute inset-0 bg-gradient-to-br from-comea-main/90 to-slate-900/95 mix-blend-multiply -z-10"
  >
  </div>

  <!-- Gradiente inferior para suavizar la transición -->
  <div
    class="absolute inset-0 bg-gradient-to-t from-dark-bg via-transparent to-transparent z-0"
  >
  </div>

  <div class="relative z-10 max-w-5xl mx-auto mt-16 animate-fade-in">
    <div
      class="inline-flex items-center gap-2 py-1 px-4 rounded-full bg-white/10 border border-white/20 backdrop-blur-md text-comea-accent text-xs font-bold tracking-widest mb-8 uppercase shadow-2xl"
    >
      <span class="w-2 h-2 rounded-full bg-comea-accent animate-pulse"></span>
      Liderazgo Aeroespacial
    </div>

    <h1
      class="text-5xl md:text-7xl lg:text-8xl font-display font-bold text-white mb-8 leading-[1.1] tracking-tight"
    >
      Impulsando el futuro <br />
      <span
        class="text-transparent bg-clip-text bg-gradient-to-r from-blue-300 via-comea-tech to-blue-500"
        >aeroespacial de México</span
      >
    </h1>

    <p
      class="text-lg md:text-2xl text-slate-100 mb-12 max-w-3xl mx-auto font-light leading-relaxed drop-shadow-md"
    >
      Unificamos academia, industria y gobierno para detonar el potencial
      tecnológico del país.
    </p>

    <div class="flex flex-col sm:flex-row gap-5 justify-center items-center">
      <a
        href="#nosotros"
        class="group relative px-8 py-4 bg-comea-main text-white font-semibold rounded-xl overflow-hidden transition-transform hover:-translate-y-1 shadow-xl shadow-blue-900/20 border border-white/10"
      >
        <span class="relative">¿Qué es COMEA?</span>
      </a>
      <a
        href="#convocatoria"
        class="px-8 py-4 bg-white/10 hover:bg-white/20 backdrop-blur-md border border-white/20 text-white font-semibold rounded-xl transition-all hover:border-white/40"
      >
        Convocatoria 2025
      </a>
    </div>
  </div>

  <div
    class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce text-white/30 z-10"
  >
    <Icon name="fa6-solid:chevron-down" class="text-2xl" />
  </div>
</section>
