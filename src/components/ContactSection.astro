---
import { Icon } from "astro-icon/components";
---

<section id="contacto" class="py-20">
  <div class="max-w-7xl mx-auto px-6 grid lg:grid-cols-2 gap-16">
    <div>
      <h1
        class="text-4xl font-display font-bold text-comea-main dark:text-white mb-6"
      >
        Ponte en contacto
      </h1>
      <p class="text-slate-600 dark:text-slate-300 mb-8">
        ¿Tienes dudas sobre afiliación, convenios o eventos? Escríbenos y
        nuestro equipo te responderá a la brevedad.
      </p>

      <!-- Datos de Contacto -->
      <div class="space-y-6 mb-10">
        <div class="flex items-start gap-4">
          <div
            class="w-10 h-10 rounded-full bg-blue-100 dark:bg-slate-800 flex items-center justify-center text-comea-main dark:text-blue-400 shrink-0 mt-1"
          >
            <Icon name="fa6-solid:location-dot" />
          </div>
          <div>
            <h4 class="font-bold text-slate-800 dark:text-slate-100">
              Oficinas Centrales
            </h4>
            <p class="text-sm text-slate-500 dark:text-slate-400">
              Av. Universidad 123, Col. Del Valle, 03100 CDMX, México
            </p>
          </div>
        </div>
        <div class="flex items-start gap-4">
          <div
            class="w-10 h-10 rounded-full bg-blue-100 dark:bg-slate-800 flex items-center justify-center text-comea-main dark:text-blue-400 shrink-0 mt-1"
          >
            <Icon name="fa6-solid:envelope" />
          </div>
          <div>
            <h4 class="font-bold text-slate-800 dark:text-slate-100">
              Correo Electrónico
            </h4>
            <p class="text-sm text-slate-500 dark:text-slate-400">
              contacto@comea.org.mx
            </p>
          </div>
        </div>
        <div class="flex items-start gap-4">
          <div
            class="w-10 h-10 rounded-full bg-blue-100 dark:bg-slate-800 flex items-center justify-center text-comea-main dark:text-blue-400 shrink-0 mt-1"
          >
            <Icon name="fa6-solid:phone" />
          </div>
          <div>
            <h4 class="font-bold text-slate-800 dark:text-slate-100">
              Teléfono
            </h4>
            <p class="text-sm text-slate-500 dark:text-slate-400">
              +52 55 1234 5678
            </p>
          </div>
        </div>
      </div>
    </div>

    <!-- Formulario -->
    <div
      class="bg-white dark:bg-dark-card p-8 rounded-2xl shadow-xl border border-slate-100 dark:border-slate-700"
    >
      <form>
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 mb-4">
          <div>
            <label
              for="nombre"
              class="block text-xs font-bold text-slate-500 uppercase mb-1"
              >Nombre</label
            >
            <input
              type="text"
              id="nombre"
              class="w-full p-3 rounded-lg bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 focus:outline-none focus:ring-2 focus:ring-comea-tech dark:text-white transition-colors"
              required
            />
          </div>
          <div>
            <label
              for="apellido"
              class="block text-xs font-bold text-slate-500 uppercase mb-1"
              >Apellido</label
            >
            <input
              type="text"
              id="apellido"
              class="w-full p-3 rounded-lg bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 focus:outline-none focus:ring-2 focus:ring-comea-tech dark:text-white transition-colors"
              required
            />
          </div>
        </div>
        <div class="mb-4">
          <label
            for="email"
            class="block text-xs font-bold text-slate-500 uppercase mb-1"
            >Email</label
          >
          <input
            type="email"
            id="email"
            class="w-full p-3 rounded-lg bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 focus:outline-none focus:ring-2 focus:ring-comea-tech dark:text-white transition-colors"
            required
          />
        </div>
        <div class="mb-4">
          <label
            for="asunto"
            class="block text-xs font-bold text-slate-500 uppercase mb-1"
            >Asunto</label
          >
          <select
            id="asunto"
            class="w-full p-3 rounded-lg bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 focus:outline-none focus:ring-2 focus:ring-comea-tech dark:text-white transition-colors"
          >
            <option>Información General</option>
            <option>Afiliación Institucional</option>
            <option>Prensa / Medios</option>
            <option>Bolsa de Trabajo</option>
          </select>
        </div>
        <div class="mb-6">
          <label
            for="mensaje"
            class="block text-xs font-bold text-slate-500 uppercase mb-1"
            >Mensaje</label
          >
          <textarea
            id="mensaje"
            rows="4"
            class="w-full p-3 rounded-lg bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 focus:outline-none focus:ring-2 focus:ring-comea-tech dark:text-white transition-colors"
            required></textarea>
        </div>
        <button
          type="submit"
          class="w-full py-4 bg-comea-main text-white font-bold rounded-lg hover:bg-comea-light transition shadow-lg shadow-blue-900/30"
          >Enviar Mensaje</button
        >
      </form>
    </div>
  </div>
</section>
